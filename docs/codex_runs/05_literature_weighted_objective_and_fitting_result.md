# Stage 05 Result

Status: completed as a quick deterministic fitting smoke path.

The literature-weighted objective now scores model LSD-minus-placebo deltas with normalized squared error, sign mismatch penalty, overshoot penalty, seed variance penalty, and a small sparsity penalty. The quick run wrote all required Stage 5 artifacts under `results/stage_5/`.

Current quick-budget best: `striatal_routing_only`, loss `0.7694874657995437`.

This result is a development smoke signal, not a final fit. Multi-seed and larger-candidate runs remain the next scientific validation step.
